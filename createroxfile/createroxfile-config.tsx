import * as React from "react";
import * as Semantic from "semantic-ui-react";

export function createroxfileconfig() {
  return <Semantic.Modal.Content>
    <div><h1>Neues Dokument einstellen.</h1></div>
  </Semantic.Modal.Content>;
}