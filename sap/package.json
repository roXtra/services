{
  "name": "@eformservice/sap",
  "servicename": "sap",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "lint": "eslint --ext .ts,.tsx ./",
    "build": "tsc",
    "test": "cross-env HOTRELOAD=no DEBUG=bpmn-engine:123* ts-mocha -p tsconfig.json ./**/*.roxtest.ts --exit --recursive --timeout 40000 --require regenerator-runtime",
    "copyproduction": "cross-var copyfiles package.json production && copyfiles .npmrc production && cd ./production && npm install --production && cd .. && cross-var copyfiles -u 1 production/node_modules/** $npm_package_servicename && rimraf production/",
    "copyfiles": "cross-var copyfiles service.json dist/* $npm_package_servicename && npm run copyproduction",
    "zip": "cross-var bestzip $npm_package_servicename.zip $npm_package_servicename",
    "copyandzip": "cross-var npm run copyfiles && npm run zip && cross-var rimraf $npm_package_servicename"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@sap/hana-client": "^2.4.155",
    "processhub-sdk": "https://github.com/roXtra/processhub-sdk/releases/download/v8.10.0-13/release.tgz",
    "react": "^16.8.6",
    "semantic-ui-react": "^0.87.2"
  },
  "devDependencies": {
    "@types/hapi__hapi": "^18.2.5",
    "@types/node": "^12.7.12",
    "@types/react": "^16.8.23",
    "@typescript-eslint/eslint-plugin": "^2.3.0",
    "bestzip": "^2.1.4",
    "copyfiles": "^2.1.1",
    "cross-env": "^6.0.0",
    "cross-var": "^1.1.0",
    "eslint": "^6.5.1",
    "mocha": "^6.2.0",
    "regenerator-runtime": "^0.13.3",
    "rimraf": "^3.0.0",
    "ts-mocha": "^6.0.0",
    "typescript": "^3.5.2"
  }
}
