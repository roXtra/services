{
  "name": "@eformservice/sap",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "config": {
    "servicename": "sap"
  },
  "scripts": {
    "lint": "eslint --ext .ts,.tsx ./",
    "build": "tsc",
    "test": "cross-env HOTRELOAD=no DEBUG=bpmn-engine:123* mocha ./dist/**/*.roxtest.js --exit --recursive --timeout 40000 --require source-map-support/register",
    "copyproduction": "cross-var copyfiles package.json production && copyfiles .npmrc production && cd ./production && npm install --production && cd .. && cross-var copyfiles -u 1 \"./production/node_modules/**/*\" $npm_package_config_servicename && rimraf production/",
    "copyfiles": "cross-var copyfiles service.json configtemplate.json dist/* $npm_package_config_servicename && npm run copyproduction",
    "zip": "cross-var bestzip $npm_package_config_servicename.zip $npm_package_config_servicename",
    "copyandzip": "cross-var npm run copyfiles && npm run zip && cross-var rimraf $npm_package_config_servicename"
  },
  "engines": {
    "node": "18",
    "npm": "9"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@sap/hana-client": "2.15.22",
    "processhub-sdk": "https://github.com/roXtra/processhub-sdk/releases/download/v9.63.0-3/release.tgz",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.1",
    "@types/node": "^18.15.11",
    "@types/react": "^18.0.31",
    "bestzip": "^2.2.1",
    "copyfiles": "^2.4.1",
    "cross-env": "^7.0.3",
    "cross-var": "^1.1.0",
    "eslint": "^8.37.0",
    "mocha": "^10.2.0",
    "rimraf": "^4.4.1",
    "ts-loader": "^9.4.2",
    "typescript": "^4.9.5"
  }
}
